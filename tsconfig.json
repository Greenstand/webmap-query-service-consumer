// This is used by `ts language service` and testing.
// Includes source and test files.
{
  "extends": "./tsconfig.base.json",
  "include": [
    "**/*.ts",
    ".jest/*.ts" // seems redundant, but still necessary
  ]
}
